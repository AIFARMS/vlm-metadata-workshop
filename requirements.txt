# Minimal install for workshop (no API keys, TF-IDF metrics only):
#   pip install -r requirements.txt
#
# Live VLM inference (classroom_vlm_comparison.py):
#   pip install -r requirements.txt -r requirements-live.txt
#
# SBERT semantic metrics (add_evaluation_metrics.py without --no-sbert):
#   pip install -r requirements.txt -r requirements-sbert.txt

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
